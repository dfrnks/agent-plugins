This file exists solely so that `tests/fixtures/reference-ok.md` has a real
`core/…md` path to reference, giving `checks/references-resolve.sh` an
accepting case to check against. It carries no pipeline content of its own;
a later task may repurpose or remove it once real `core/` files exist.
