# Structure fixture: heading lookalike

The `## Required Heading` section is named here in prose but never declared,
so a checker matching an unanchored substring accepts this file while the
heading it demanded does not exist.

## A Different Heading

Body text.
